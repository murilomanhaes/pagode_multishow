<div id="agenda">
  <h2><b>Consultar e Relatório</b> agenda</h2><br/>
  
  <div id='consulta'> 
   <%= form_tag consultar_agendas_path, method: :get do %>
     <%= label_tag :data, 'Busca por data' %>
     <%= text_field_tag :data %>
     <%= submit_tag 'Buscar' %>
   <% end %>
  </div>

  <div id = "relatorioagenda">
    <table>
      <tr>
        <th>Data</th>
        <th>Descrição</th>
        <th>Horário</th>
        <th>Tipo</th>
      </tr>
      

    <% @agendas.each do |agenda| %>
      <tr>
        <td><%= agenda.data.to_s_br %></td>
        <td><%= agenda.descricao %></td>
        <td><%= agenda.horario %></td>
        <td><%= agenda.tipo %></td>
      </tr>
    <% end %>
    </table>
   
   </div>
    
</div>

   <%= link_to '', consultar_relatorio_administracoes_path, :class => "voltar"%>
