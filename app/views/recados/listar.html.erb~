<div id="novidadeadmin">
  <h2><b>Mural de </b> Recado</h2><br/>
  
  <div id = "relatorionovidade">
    <table>
      <tr>
        <th>Nome</th>
        <th>Recado</th>
        <th>Data</th>
      </tr>
      

    <% @recados.each do |recado| %>
      <tr>
        <td class="tabelatamanho">
        <% if can? :manage, :all %>
          <%= link_to recado.usuario.nome, usuario_path(recado.usuario) %>
        <% else %>
          <%= recado.usuario.nome %>
        <% end %>
        </td>
        
        <td><%= recado.recado %></td>
        <td class="tabelatamanho"><%= recado.data.to_s_br %></td>
      </tr>
    <% end %>
    </table>
      
   </div>
        
</div>
<%= link_to 'New Recado', new_recado_path %>


