<div id="recadoadmin">
  <h2><b>Usuários </b> cadastrados</h2><br/>
  
  <div id = "relatoriorecado">
     <% @usuarios.each do |usuario| %>
        <% if can? :manage, :all %>
          <%= usuario.id %> - <%= link_to usuario.nome, usuario_path(usuario) %>
        <% else %>
          <%= usuario.id %> - <%= usuario.nome %></p>
        <% end %>
     <% end%>
  </div>
</div>
